# StringMatcher

A simple TDD practice project for implementing string similarity matching.

## Build

* C++
* GoogleTest
* GoogleMock

## Features (WIP)

* Compare two strings
* Calculate string similarity

## Input Rules

* Only uppercase alphabet characters (A-Z) are supported.
* Any other input throws `InvalidStringException`.

## Run Tests

Build the project and run the test executable.
